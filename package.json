{
  "name": "banana-cli",
  "version": "0.1.0",
  "description": "CLI for Gemini image generation and image editing prompts.",
  "type": "module",
  "bin": {
    "banana": "./bin/banana.js"
  },
  "files": [
    "bin",
    "src",
    "README.md"
  ],
  "scripts": {
    "start": "node ./bin/banana.js",
    "test": "node --test",
    "lint": "node --check ./bin/banana.js && node --check ./src/index.js && node --check ./test/index.test.js"
  },
  "keywords": [
    "cli",
    "gemini",
    "image-generation",
    "banana"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yargs": "^17.7.2"
  }
}
